const colors = require('../styles/colors')

module.exports = {
  theme: {
    extend: {
      colors: {
        primary: colors.orange,
        error: colors.marigold,
        success: colors.lime,
        warning: colors.marigold,
        gray: colors.gray
      }
    }
  }
}
