const colors = require('../styles/colors')

module.exports = {
  theme: {
    extend: {
      colors: {
        primary: colors.marigold,
        error: colors.magenta,
        success: colors.lime,
        warning: colors.magenta,
        gray: colors.gray
      }
    }
  }
}
