import { fonts } from '../../theme/foundations/typography';

const Stat = {
  baseStyle: {
    number: {
      fontFamily: fonts.heading,
      fontWeight: 'medium'
    },
    label: {
      fontWeight: 'medium'
    }
  }
};

export default Stat;
