import { colors } from '../omui.framerfx/code/canvas';

const space: string[] = ['0', '4px', '8px', '16px', '32px'];

const font = '-apple-system, BlinkMacSystemFont, Helvetica, Arial, sans-serif';

export const theme = {
  color: colors,
  font,
  space,
};
